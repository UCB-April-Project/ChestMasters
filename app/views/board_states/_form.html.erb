<%= form_with(model: board_state, local: true) do |form| %>
  <% if board_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_state.errors.count, "error") %> prohibited this board_state from being saved:</h2>

      <ul>
      <% board_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
